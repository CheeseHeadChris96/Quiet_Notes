const {test}=require('node:test');const assert=require('node:assert/strict');const Images=require('../src/image-support.js');const Store=require('../src/store.js');
test('embedded image sources accept supported formats and reject remote or executable sources',()=>{for(const type of ['png','jpeg','webp','gif'])assert.equal(Images.safeSource(`data:image/${type};base64,YWJj`),true);for(const src of ['https://example.com/photo.png','file:///tmp/photo.png','data:image/svg+xml;base64,YWJj','javascript:alert(1)','data:image/png;base64,YWJj\" onerror=alert(1)'])assert.equal(Images.safeSource(src),false);assert.equal(Images.safeSource('data:image/png;base64,'+'a'.repeat(Images.MAX_EMBEDDED_LENGTH)),false);});
test('note validation retains embedded images for backups and restores',()=>{const html='<img src="data:image/png;base64,YWJj" alt="Photo">';const note={id:'photo',title:'Photo',body:'',html,folderId:'generic',color:'neutral',created:1,updated:1};assert.equal(Store.validate(JSON.parse(JSON.stringify([note])))[0].html,html);});
test('saved image widths only accept bounded whole pixels',()=>{for(const width of [1,32,'640',4096])assert.equal(Images.safeWidth(width),Number(width));for(const width of [null,'',0,-1,4097,'20px','50%','12.5','1e3','Infinity','20; color:red'])assert.equal(Images.safeWidth(width),null);});
test('corner drags preserve proportions and clamp to the editor',()=>{for(const [corner,dx,dy] of [['se',80,60],['ne',80,-60],['sw',-80,60],['nw',-80,-60]])assert.equal(Images.resizedWidth(160,120,dx,dy,corner,800),240);assert.equal(Images.resizedWidth(160,120,-1000,-1000,'se',800),32);assert.equal(Images.resizedWidth(160,120,1000,1000,'se',500),500);});
